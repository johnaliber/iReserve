services:
  report-service:
    build:
      context: .
      dockerfile: report-service/Dockerfile
    environment:
      REPORT_SERVICE_SECRET: ${REPORT_SERVICE_SECRET}
    ports:
      - "8787:8787"
    restart: unless-stopped

